# Copy to .env.local for local development
# In production (Vercel), set these in the project settings

# The base URL of your deployed FastAPI backend (no trailing slash)
VITE_API_URL=https://your-api.onrender.com
